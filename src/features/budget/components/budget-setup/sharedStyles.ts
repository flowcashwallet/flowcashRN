import { Spacing } from "@/constants/theme";
import { StyleSheet } from "react-native";

/** Contenedor de cada paso del wizard — mismo valor en los tres pasos. */
export const stepStyles = StyleSheet.create({
  stepContainer: {
    marginBottom: Spacing.xl,
  },
});
